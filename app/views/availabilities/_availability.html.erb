<li>
  <%= availability.find_town_from(availability.town_id) %>
    - <%= availability.find_charity_from(availability.charity_id) %>
    - <%= availability.date.strftime("%a, %B %d, %Y") %>
  <%= button_to "Schedule a Donation", 
    new_availability_pickup_path(availability), method: :get %>
</li>
